/*
 * Placeholder for the "runtime" environment variables.
 * If the UI is served by the backend, this file will never get called.
 * Instead, the UI will use env.js that provided by the backend.
 * Otherwise, it will use "compile" environment variables specified in src/config.js.
 */
